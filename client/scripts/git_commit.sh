# add all changes
git add .

# add a commit message
commit_message="updated"
git commit -m "$commit_message"

# print a message in the console (changes commited in git log)
echo "changes commited successfully" gitpush.sh
